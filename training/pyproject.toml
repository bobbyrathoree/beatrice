[project]
name = "beatrice-ml"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "numpy>=1.26", "soundfile>=0.12", "librosa>=0.10",
  "torch>=2.3", "pyyaml>=6.0",
]
[dependency-groups]
dev = ["pytest>=8.0"]
[tool.pytest.ini_options]
testpaths = ["tests"]
